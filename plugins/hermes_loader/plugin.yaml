name: vizier_tools
version: "1.0.0"
description: "Vizier governed production engine — run_governed pipeline + span-based observability"
provides_tools:
  - run_pipeline
  - query_logs
provides_hooks:
  - pre_tool_call
  - pre_llm_call
  - post_llm_call
  - on_session_start
  - on_session_end
